info:
  name: POST
  type: http
  seq: 5

http:
  method: POST
  url: http://127.0.0.1:8000/api/classrooms
  body:
    type: json
    data: |-
      {
        "codigo": "INF-101"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
